#include "main.h"

/**
* _strlen - will give the length of a string
*
* @i: is the given string to count
*
* Return: is the length of string
*/

int _strlen(char *i)
{
	int length = 0;

	while
	(i[length] != '\0') length++;
	return (length);
}
