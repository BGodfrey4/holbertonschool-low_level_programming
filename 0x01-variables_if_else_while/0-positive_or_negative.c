#include <stdlib.h>
#include <time.h>
#include <stdio.h>
/** 
 * main - assigns random variable
 * @n: is a variable
 * Return: 0*
 */
int main(void)
{
	int n;

	srand(time(0));
	n = rand() - RAND_MAX / 2;
	(n = 0); (n < 0); (n < 0);
	return (0);
}
